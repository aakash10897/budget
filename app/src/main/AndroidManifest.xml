<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manukanu.expenditure">
    <uses-sdk android:minSdkVersion="8"/>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>


    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".welcome"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signup" />
        <activity android:name=".login" />
        <activity android:name=".add_money" />
        <activity android:name=".BudgetDialog" />
        <activity android:name=".edit_fields" />
        <activity android:name=".password_change" />
        <activity
            android:name=".moneyused"
            android:label="@style/AppTheme"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".username_change" />
        <activity android:name=".edit_budget" />
        <activity android:name=".edit_mail"></activity>

        <activity
            android:name=".listview"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        -<activity android:name=".AlarmActivity" android:label="@string/app_name">
    </activity>
        -<activity android:name=".Reminder_dialog" android:label="@string/app_name">
    </activity>
        <activity android:name=".SecondActivity"/>

        <service android:name=".ScheduleService"/>

        <service android:name=".NotifyService" android:exported="true" android:enabled="true"/>

    </application>

</manifest>